{
  "name": "node-crc",
  "version": "2.1.0",
  "description": "To compute CRC values by providing the length of bits, expression, reflection, an initial value and a final xor value. It has many built-in CRC functions.",
  "main": "node-crc.node",
  "typings": "lib/lib.d.ts",
  "scripts": {
    "test": "mocha -r ts-node/register tests/**/*.test.ts",
    "build": "npm run build:neon && npm run build:ts",
    "build:neon": "cargo-cp-artifact -nc index.node -- cargo build --release --message-format=json-render-diagnostics",
    "build:ts": "tsc",
    "build:watch": "tsc -w",
    "lint": "eslint src tests",
    "prebuild": "napi build --release",
    "prepublishOnly": "npm run prebuild && npm run lint && npm run test"
  },
  "napi": {
    "name": "node-crc"
  },
  "engines": {
    "node": ">=12"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/magiclen/node-crc.git"
  },
  "keywords": [
    "node.js",
    "crc",
    "crc8",
    "crc16",
    "crc32",
    "crc64"
  ],
  "author": "Magic Len <len@magiclen.org> (https://magiclen.org)",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/magiclen/node-crc/issues"
  },
  "homepage": "https://magiclen.org/node-js-crc/",
  "dependencies": {
    "@types/node": "^17.0.32"
  },
  "devDependencies": {
    "@napi-rs/cli": "^2.10.0",
    "@types/chai": "^4.3.1",
    "@types/mocha": "^9.1.1",
    "@typescript-eslint/eslint-plugin": "^5.23.0",
    "@typescript-eslint/parser": "^5.23.0",
    "cargo-cp-artifact": "^0.1.6",
    "chai": "^4.3.6",
    "eslint": "^8.15.0",
    "mocha": "^10.0.0",
    "ts-node": "^10.7.0",
    "typescript": "^4.6.4"
  }
}
